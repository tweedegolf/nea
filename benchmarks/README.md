# Nea Benchmarks

## Tokio

```shell
cd rust-tokio

cargo run --example unfavorable
cargo run --example average
cargo run --example favorable
```

## Node

```shell
cd ts-node

# Compile TypeScript
npx tsc

node . unfavorable
node . average
node . favorable
```
